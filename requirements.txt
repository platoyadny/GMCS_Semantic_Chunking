# bank_chunker (все зависимости обязательны)
openpyxl>=3.1.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
python-dotenv>=1.0.0

# asis_chunker (чанкинг работает без внешних зависимостей — только stdlib;
#               langchain-openai и python-dotenv нужны только для --use-llm)
